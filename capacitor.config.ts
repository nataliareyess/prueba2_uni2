import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph2.u2.nataliar',
  appName: 'ph2-u2-nataliar',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
